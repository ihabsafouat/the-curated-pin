[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[functions."email-sequences"]
  schedule = "@hourly"

[functions."maintenance"]
  schedule = "@daily"
