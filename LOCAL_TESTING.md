# The Curated Pin — local testing

Latest recovered site source prepared for local testing. The image-supplied blog update was cancelled; existing article and Pinterest content is preserved.

Run `npm install`, then `npm run dev`, and open http://localhost:3000. Node.js 22.13+ is required.
